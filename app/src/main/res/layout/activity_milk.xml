<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Contenido principal con barra y scroll -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar si lo necesitas (puedes integrarlo con ActionBar) -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:title="Información sobre la Leche"
            android:titleTextColor="#FFFFFF" />

        <!-- Aquí va tu contenido -->
        <ScrollView
            android:id="@+id/mainScroll"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#FFFDE7">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- Título -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="La Leche: Propiedades y Curiosidades"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:textColor="#5D4037"
                    android:layout_marginBottom="16dp" />

                <!-- SECCIÓN: PARÁMETROS -->
                <TextView
                    android:id="@+id/sectionParametros"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="📊 Parámetros Físico-Químicos"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="8dp" />

                <!-- ... el resto de secciones como las tienes ... -->

                <!-- SECCIÓN: SIMULADOR -->
                <TextView
                    android:id="@+id/sectionSimulador"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🧪 Simulador Básico"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:layout_marginTop="24dp" />

                <!-- SECCIÓN: COMPARATIVA -->
                <TextView
                    android:id="@+id/sectionComparativa"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🥛 Comparativa de Tipos de Leche"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="#3E2723"
                    android:layout_marginTop="24dp"
                    android:layout_marginBottom="8dp" />

                <!-- Botón volver -->
                <Button
                    android:id="@+id/btnVolver"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Volver"
                    android:layout_marginTop="32dp"
                    android:backgroundTint="#FFCDD2"
                    android:textColor="#B71C1C" />
            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- Drawer lateral -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#FFFFFF"
        app:menu="@menu/menu_drawer"
        app:itemIconSize="24dp"
        />

</androidx.drawerlayout.widget.DrawerLayout>
